Installation
============

Installing Taskverse
-----------------------
Clone the repo:

.. code-block::

    git clone git@github.com:helen9975/[repo-name]

``cd`` into the repo and run 

.. code-block::
    
    pip install .